# 🗺️ ROADMAP v3.0 - AI Companion Evolution

**Статус:** После успешной модернизации (Gemini API + ChromaDB + Initiative Engine)
**Дата обновления:** 2025-11-12
**Фокус:** Естественность взаимодействия и умная виртуальная жизнь

---

## ✅ Что уже реализовано

### 🎉 Недавние достижения (v3.0)

#### 1. **Миграция на Gemini 2.5 Flash**
- ✅ Полная замена OpenRouter на Google Gemini API
- ✅ Три режима работы: DIALOGUE, PLANNING, ANALYTICS
- ✅ Автоматическая конфигурация параметров по типу задачи
- ✅ Статистика использования по категориям
- ✅ Бесплатный tier: 10 req/min, 250k tokens/min

**Файл:** `app/core/gemini_api_manager.py`

#### 2. **Векторная память ChromaDB**
- ✅ Семантический поиск по воспоминаниям
- ✅ 3 коллекции: conversations, memories, emotional_memories
- ✅ Фильтрация по важности, эмоциям, тегам
- ✅ Гибридный подход: векторы + SQL fallback
- ✅ ~60% экономия токенов через умный контекст

**Файл:** `app/database/vector_memory_manager.py`

#### 3. **Промпт-менеджер на Jinja2**
- ✅ Вынесение всех промптов в шаблоны
- ✅ 5 темплейтов: dialogue, initiative, planning, humanize_activity, planning_commands
- ✅ Сокращение с ~800 до ~300 строк кода промптов
- ✅ Легкая настройка без правки кода
- ✅ Кэширование скомпилированных шаблонов

**Директория:** `prompts/`
**Файл:** `app/core/prompt_manager.py`

#### 4. **Умная система инициатив**
- ✅ InitiativeEngine с 8-факторной моделью
- ✅ Вероятность зависит от: времени, настроения, энергии, активности, близости, времени суток, дня недели
- ✅ Контекстные триггеры: завершённые активности, долгое молчание, особое настроение
- ✅ Тематический выбор на основе контекста
- ✅ Естественные инициативы вместо таймеров

**Файл:** `app/core/initiative_engine.py`

#### 5. **Базовые системы**
- ✅ Психологическая модель (Big Five)
- ✅ Система отношений с трекингом близости
- ✅ Виртуальная жизнь с планированием дня
- ✅ Telegram интеграция с реалистичной печатью
- ✅ SQLite + ChromaDB гибридная БД
- ✅ APScheduler для периодических задач

---

## 🎯 Ближайшие цели (Приоритет: ВЫСОКИЙ)

### 🔥 **1. Динамическое планирование активностей**

**Проблема:** Сейчас планы генерируются по шаблону (9-17 работа, 18-22 свободна)

**Цель:** ИИ должен сам планировать день с учётом:
- Дня недели (воскресенье ≠ работа)
- Настроения и энергии
- Памяти о предыдущих днях
- Персонажа и его интересов

**Рекомендации:**

1. **Расширить `daily_planning_system.py`:**
   ```python
   # Добавить контекст в промпт планирования
   context = {
       'day_of_week': datetime.now().strftime('%A'),
       'is_weekend': datetime.now().weekday() >= 5,
       'yesterday_activities': self.get_yesterday_summary(),
       'mood': character_state['mood'],
       'energy': character_state['energy_level'],
       'character_interests': character_profile['interests']
   }
   ```

2. **Обновить `prompts/planning.jinja2`:**
   - Добавить условия для выходных
   - Учитывать настроение (sad → домашние активности)
   - Разнообразие через рандомизацию

3. **Добавить flexibility в БД:**
   ```sql
   ALTER TABLE virtual_activities ADD COLUMN flexibility INTEGER DEFAULT 5;
   ALTER TABLE virtual_activities ADD COLUMN can_reschedule BOOLEAN DEFAULT 1;
   ```

**Ожидаемая сложность:** Средняя (3-5 дней)
**Приоритет:** 🔥 Критический

---

### 🌡️ **2. Динамика настроения от событий**

**Проблема:** Настроение меняется случайно, не реагирует на события

**Цель:** Настроение должно изменяться от:
- Событий в диалоге (комплимент → happy, критика → sad)
- Завершённых активностей (успешная работа → proud)
- Времени суток (утро → energetic, поздний вечер → tired)
- Отношений (близость растёт → content)

**Рекомендации:**

1. **Создать `app/core/mood_engine.py`:**
   ```python
   class MoodEngine:
       def __init__(self):
           self.mood_modifiers = {
               'positive_message': +0.5,
               'negative_message': -0.8,
               'long_conversation': +0.3,
               'ignored_by_user': -0.5,
               'completed_activity': +0.2
           }

       def calculate_mood_change(self, event_type, current_mood, personality):
           # Учитывать neuroticism из Big Five
           volatility = personality['neuroticism'] / 10
           base_change = self.mood_modifiers.get(event_type, 0)
           return base_change * volatility
   ```

2. **Интегрировать в `companion.py`:**
   ```python
   # После обработки сообщения
   sentiment = self.analyze_sentiment(user_message)
   mood_change = self.mood_engine.calculate_mood_change(
       event_type='positive_message' if sentiment > 0 else 'negative_message',
       current_mood=self.psychology.current_mood,
       personality=self.character_profile['personality_traits']
   )
   self.psychology.adjust_mood(mood_change)
   ```

3. **Добавить анализ sentiment через Gemini:**
   - Быстрый запрос с temperature=0.3 (детерминированный)
   - Возврат: {"sentiment": "positive|neutral|negative", "intensity": 0.0-1.0}
   - Кэширование для похожих сообщений

**Ожидаемая сложность:** Средняя (4-6 дней)
**Приоритет:** 🔥 Высокий

---

## 🚀 Будущие улучшения (Приоритет: СРЕДНИЙ)

### 📅 **3. Долгосрочная память и мечты**

**Идея:** ИИ помнит свои желания и планирует на будущее

**Рекомендации:**
- Таблица `future_desires` для хранения желаний
- Еженедельная генерация целей (каждое воскресенье)
- Напоминания о старых желаниях в диалогах
- Приоритизация: важные vs спонтанные желания

**Файлы:** `app/database/schema.sql`, `app/core/desires_manager.py` (новый)

**Сложность:** Высокая (2-3 недели)

---

### 🌐 **4. Web UI для настройки**

**Идея:** Визуальный интерфейс для создания и редактирования персонажей

**Рекомендации:**

**Frontend:**
- React/Vue.js SPA
- Формы для редактирования JSON персонажей
- Live preview диалогов
- Визуализация статистики (токены, инициативы, настроение)

**Backend API:**
```python
# Новые эндпоинты в Flask
@app.route('/api/characters', methods=['GET', 'POST'])
@app.route('/api/characters/<id>', methods=['PUT', 'DELETE'])
@app.route('/api/stats', methods=['GET'])
@app.route('/api/test-dialogue', methods=['POST'])  # Live preview
```

**Технологии:**
- Flask для API (уже есть в зависимостях)
- React для фронтенда
- Socket.io для live chat preview

**Сложность:** Высокая (3-4 недели)
**Приоритет:** Средний (после завершения динамики)

---

### 🌍 **5. Мультиязычность (English support)**

**Идея:** Поддержка английского языка

**Рекомендации:**

1. **Структура:**
   ```
   prompts/
   ├── ru/
   │   ├── dialogue.jinja2
   │   └── initiative.jinja2
   └── en/
       ├── dialogue.jinja2
       └── initiative.jinja2
   ```

2. **Автоопределение языка:**
   ```python
   # В companion.py
   def detect_language(self, text):
       # Простая эвристика: кириллица = русский
       has_cyrillic = bool(re.search('[а-яА-Я]', text))
       return 'ru' if has_cyrillic else 'en'
   ```

3. **Gemini отлично работает с английским:**
   - Промпты переводятся 1:1
   - Personality traits остаются числами (универсальны)
   - Только контент темплейтов на английском

**Сложность:** Низкая (1-2 недели)
**Приоритет:** Низкий (не срочно)

---

### 💬 **6. Discord интеграция**

**Идея:** Добавить Discord бота (помимо Telegram)

**Рекомендации:**

1. **Создать `app/integrations/discord_bot.py`:**
   ```python
   import discord
   from discord.ext import commands

   class DiscordCompanionBot(commands.Bot):
       def __init__(self, companion_instance):
           super().__init__(command_prefix='!')
           self.companion = companion_instance

       @commands.Cog.listener()
       async def on_message(self, message):
           if message.author == self.user:
               return
           response = await self.companion.process_message(
               user_id=message.author.id,
               text=message.content
           )
           await message.channel.send(response)
   ```

2. **Особенности Discord:**
   - Markdown поддержка (жирный, курсив)
   - Эмодзи как в Telegram
   - Typing indicator через `async with channel.typing()`
   - Поддержка серверов (guilds) - фильтрация по allowed_guilds

**Зависимости:** `discord.py>=2.0`

**Сложность:** Низкая (3-5 дней)
**Приоритет:** Низкий

---

### 🎙️ **7. Голосовые сообщения**

**Идея:** Поддержка voice messages в Telegram

**Рекомендации:**

1. **Speech-to-Text (STT):**
   - Google Cloud Speech-to-Text API
   - OpenAI Whisper API (платный, но качественный)
   - Vosk (open-source, оффлайн, бесплатный)

2. **Text-to-Speech (TTS):**
   - Google Cloud TTS (наиболее естественный русский)
   - ElevenLabs (дорого, но очень реалистично)
   - Silero TTS (open-source, приемлемое качество)

3. **Workflow:**
   ```python
   # В telegram_bot.py
   @bot.voice_handler()
   async def handle_voice(update, context):
       voice_file = await update.message.voice.get_file()
       voice_bytes = await voice_file.download_as_bytearray()

       # STT
       text = await stt_service.transcribe(voice_bytes, language='ru')

       # Обработка
       response_text = await companion.process_message(user_id, text)

       # TTS
       response_audio = await tts_service.synthesize(response_text, voice='ru-RU-female')

       await update.message.reply_voice(response_audio)
   ```

**Зависимости:** `google-cloud-speech`, `google-cloud-texttospeech`

**Сложность:** Средняя (1-2 недели)
**Приоритет:** Средний (после Web UI)

---

## 🛠️ Технические рекомендации

### 📦 Архитектурные улучшения

#### 1. **Кэширование ответов**
```python
# app/core/response_cache.py
from functools import lru_cache
import hashlib

class ResponseCache:
    def __init__(self, max_size=100):
        self.cache = {}

    def get_cache_key(self, prompt, context):
        # Хэш от промпта + ключевые параметры контекста
        key_data = f"{prompt}:{context.get('mood')}:{context.get('energy')}"
        return hashlib.md5(key_data.encode()).hexdigest()

    def get(self, prompt, context):
        key = self.get_cache_key(prompt, context)
        return self.cache.get(key)

    def set(self, prompt, context, response):
        key = self.get_cache_key(prompt, context)
        self.cache[key] = response
```

**Применение:** Кэширование ответов на частые вопросы ("как дела?", "что делаешь?")

---

#### 2. **Оптимизация токенов**

**Текущая экономия:** ~60% через векторный поиск

**Дополнительные меры:**

1. **Сжатие истории диалога:**
   ```python
   # Вместо полной истории - краткое резюме
   def summarize_conversation_history(messages):
       if len(messages) > 10:
           # Gemini запрос на суммаризацию
           summary = await api_manager.make_request(
               APIUsageType.ANALYTICS,
               messages=[{
                   "role": "user",
                   "content": f"Summarize this conversation in 2-3 sentences: {messages}"
               }]
           )
           return summary
       return "\n".join(messages)
   ```

2. **Динамический max_tokens:**
   ```python
   # Короткие ответы в обычных диалогах
   # Длинные только когда нужно (планирование, storytelling)
   max_tokens = 200 if is_simple_question else 400
   ```

3. **Batch обработка планов:**
   ```python
   # Планировать на 3 дня сразу (реже запросов)
   plans = await generate_plans_for_days(days=3)
   ```

---

#### 3. **Мониторинг и алертинг**

**Создать `app/monitoring/stats_collector.py`:**

```python
class StatsCollector:
    def __init__(self):
        self.metrics = {
            'api_requests': 0,
            'api_errors': 0,
            'tokens_used': 0,
            'average_response_time': 0,
            'initiatives_sent': 0,
            'user_messages': 0
        }

    def log_api_request(self, duration, tokens, success):
        # Запись метрик
        pass

    def get_daily_report(self):
        # JSON отчёт за день
        return {
            'total_requests': self.metrics['api_requests'],
            'error_rate': self.metrics['api_errors'] / max(self.metrics['api_requests'], 1),
            'avg_tokens_per_request': self.metrics['tokens_used'] / max(self.metrics['api_requests'], 1)
        }
```

**Интеграция:**
- Логирование каждого API запроса
- Ежедневные отчёты в Telegram админу
- Алерты при превышении лимитов

---

#### 4. **Тестирование**

**Создать `tests/` директорию:**

```
tests/
├── test_gemini_api_manager.py
├── test_initiative_engine.py
├── test_vector_memory.py
├── test_mood_engine.py
└── test_daily_planning.py
```

**Пример теста:**

```python
# tests/test_initiative_engine.py
import pytest
from app.core.initiative_engine import InitiativeEngine

def test_high_probability_when_long_silence():
    engine = InitiativeEngine(config)

    should_send, prob, reason = engine.should_send_initiative(
        character_state={'mood': 'happy', 'energy_level': 80, 'intimacy': 70},
        virtual_life_context={'activity': 'free'},
        last_message_time=datetime.now() - timedelta(hours=8),  # 8 hours
        relationship={'intimacy_level': 75}
    )

    assert prob > 0.5, "Probability should be high after long silence"
    assert should_send == True
```

**Запуск:** `pytest tests/`

---

### 🔒 Безопасность

#### 1. **Rate limiting для API**
```python
from datetime import datetime, timedelta

class RateLimiter:
    def __init__(self, max_requests_per_minute=10):
        self.max_requests = max_requests_per_minute
        self.requests = []

    def can_make_request(self):
        now = datetime.now()
        # Удалить запросы старше 1 минуты
        self.requests = [r for r in self.requests if now - r < timedelta(minutes=1)]
        return len(self.requests) < self.max_requests

    def record_request(self):
        self.requests.append(datetime.now())
```

#### 2. **Валидация входных данных**
```python
def validate_message(text):
    # Максимальная длина
    if len(text) > 4000:
        raise ValueError("Message too long")

    # Запрещённые символы
    if any(char in text for char in ['<script>', 'eval(', 'exec(']):
        raise ValueError("Invalid characters")

    return text
```

#### 3. **Шифрование API ключей**
```python
# Использовать python-dotenv + .env файл
# Никогда не коммитить API ключи в git
import os
from dotenv import load_dotenv

load_dotenv()
api_key = os.getenv('GEMINI_API_KEY')
```

---

## 📊 Метрики качества

### KPI для оценки улучшений

| Метрика | Текущее | Цель | Приоритет |
|---------|---------|------|-----------|
| Токены на диалог | ~800 | <500 | 🔥 Высокий |
| Время ответа | ~3с | <2с | Средний |
| Инициатив/день | 3-5 | 6-8 | 🔥 Высокий |
| Разнообразие планов | Низкое | Высокое | 🔥 Высокий |
| Естественность настроения | 6/10 | 9/10 | 🔥 Высокий |
| Ошибки API | <1% | <0.1% | Средний |

---

## 🎯 Приоритизация

### Что делать в первую очередь:

**Неделя 1-2:**
1. ✅ Динамическое планирование активностей (СДЕЛАТЬ)
2. ✅ Динамика настроения от событий (СДЕЛАТЬ)

**Неделя 3-4:**
3. Тестирование и стабилизация
4. Сбор метрик и анализ

**Месяц 2:**
5. Долгосрочная память и мечты
6. Web UI (если нужен)

**Месяц 3+:**
7. Мультиязычность (по желанию)
8. Discord интеграция (по желанию)
9. Голосовые сообщения (по желанию)

---

## ⚠️ Технические долги

### Что стоит рефакторить (когда будет время):

1. **`companion.py` слишком большой (>1000 строк)**
   - Разбить на модули: messaging, planning, initiatives
   - Вынести вспомогательные функции в utils

2. **Отсутствие type hints в старом коде**
   - Добавить типизацию постепенно
   - Использовать mypy для проверки

3. **Дублирование логики форматирования**
   - Создать `app/utils/formatters.py`
   - Централизовать форматирование времени, эмодзи, markdown

4. **Нет миграций для БД**
   - Использовать Alembic для SQLite миграций
   - Версионирование схемы БД

---

## 🎉 Заключение

**Текущий статус:** Сильная база после модернизации v3.0

**Главные преимущества:**
- ✅ Мощный Gemini API (бесплатно!)
- ✅ Умный поиск памяти через ChromaDB
- ✅ Естественные инициативы через InitiativeEngine
- ✅ Гибкие промпты через Jinja2

**Следующие шаги:**
1. Динамическое планирование → живые, разнообразные дни
2. Динамика настроения → эмоциональная связь с событиями
3. Тестирование → стабильность и надёжность

**Долгосрочная цель:**
Создать максимально естественного AI-компаньона, который живёт своей жизнью, реагирует на события и строит глубокие отношения с пользователем.

---

**📅 Обновлено:** 2025-11-12
**📝 Версия:** 3.0 (Post-Modernization)
**👤 Статус:** Production Ready (базовые функции) + Active Development (улучшения)
